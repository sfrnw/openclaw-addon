# 🧪 Тестирование OpenClaw после установки

## ✅ Быстрый тест

```bash
# 1. Проверь что аддон запущен
ha addons info openclaw | grep state

# Должно быть: state: started

# 2. Проверь логи
ha addons logs openclaw | tail -20

# Должно быть: 🦞 OpenClaw is ready!

# 3. Проверь порты
netstat -tlnp | grep -E '18789|11434'

# Должны слушаться оба порта

# 4. Проверь Ollama
curl http://localhost:11434/api/tags

# Должен вернуть JSON со списком моделей

# 5. Проверь Gateway
curl http://localhost:18789/health

# Должен вернуть статус
```

---

## 📱 Тест Telegram

1. Открой бота в Telegram
2. Напиши `/start`
3. Напиши `привет`
4. Напиши `сколько будет 2+2?`
5. Напиши `расскажи короткую историю`

**Ожидай ответы в течение 5-30 секунд** (модель локальная, не облако)

---

## 🧠 Тест модели

```bash
# Через SSH в аддон (если есть доступ)
ha addons exec openclaw --command "curl http://localhost:11434/api/generate -d '{\"model\": \"phi3:mini\", \"prompt\": \"Hello!\"}'"
```

Или просто спроси что-нибудь в Telegram:
- "какая сегодня погода?" (должен сказать что не знает)
- "напиши стих про котов"
- "объясни квантовую физику простыми словами"

---

## 📊 Мониторинг ресурсов

```bash
# Память
free -h

# Docker контейнеры
docker stats --no-stream

# Ollama specifically
docker exec openclaw ps aux | grep ollama
```

**Нормальное потребление:**
- Ollama: ~200MB + ~2.5GB на модель
- OpenClaw: ~300-500MB
- Всего: ~3-3.5GB

---

## 🐛 Диагностика проблем

### Бот не отвечает

```bash
# Проверь Telegram конфиг
ha addons exec openclaw --command "cat /data/openclaw.json | jq .channels.telegram"

# Проверь allowlist
ha addons exec openclaw --command "cat /data/credentials/telegram-allowFrom.json"
```

### Модель тормозит

```bash
# Проверь загрузку CPU
top

# Проверь память
free -h
```

Если >90% RAM использовано — модель слишком большая, попробуй `qwen2.5-coder:1.5b`

### Ollama не работает

```bash
# Перезапусти аддон
ha addons restart openclaw

# Смотри логи
ha addons logs openclaw | grep -i ollama
```

---

## ✅ Чеклист успешного теста

- [ ] Аддон запущен (state: started)
- [ ] В логах "🦞 OpenClaw is ready!"
- [ ] Порт 18789 слушается
- [ ] Порт 11434 слушается
- [ ] Ollama возвращает список моделей
- [ ] Gateway отвечает на /health
- [ ] Telegram бот отвечает на /start
- [ ] Telegram бот отвечает на сообщения
- [ ] Потребление памяти < 4GB
- [ ] Home Assistant работает нормально

---

**Если всё зелёное — ПОЗДРАВЛЯЮ! 🦞 работает!**
